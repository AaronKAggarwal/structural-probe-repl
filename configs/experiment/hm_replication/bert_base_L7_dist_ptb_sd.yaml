# configs/experiment/hm_replication/bert_base_L7_dist_ptb_sd.yaml
defaults:
  - _self_
  - override /dataset: ptb_sd_official # From previous setup
  - override /embeddings: bert_base_L7_ptb_sd # Points to the new embedding config
  - override /probe: distance_probe_bert_base_fullrank # Or your chosen rank config
  - override /training: training_hm_replication # H&M hyperparameters
  - override /evaluation: eval_hm_metrics # H&M evaluation settings
    
logging:
  experiment_name: "H&M_Repl_BERTbase_L7_Dist_PTBSD"
  wandb:
    enable: true
    project: "structural-probes-hm-replication" # Or your preferred project name
    entity: "aggarwal-k-aaron" # Your W&B entity
    tags: ["hm_replication", "bert-base", "layer7", "distance", "ptb_sd"]
  enable_plots: true # Good to see plots for this important run