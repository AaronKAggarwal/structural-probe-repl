# configs/experiment/hm_style_baselines/ud_ewt/bert/dist/L7.yaml

name: hm_style_baselines/ud_ewt/bert/dist/L7

defaults:
  - _self_
  - /dataset: ud_ewt/ud_english_ewt_full
  - /embeddings: ud_ewt/bert-base-cased/L7
  - /probe: distance
  - /probe_rank@probe: "768" # BERT-base dim
  - /training: training_hm_replication # Apply H&M training hyperparameters
  - /evaluation: eval_hm_metrics # Apply H&M evaluation settings (xpos, no length filter)
  - /runtime: mps # Assuming 'mps' as your default runtime
  - /logging: hm_ud_ewt_dist_L7

logging:
  experiment_name: "HM_Style_UDEWT_BERT_L7_Dist_R768"