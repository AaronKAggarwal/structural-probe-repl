# Auto-generated by generate_configs.py
name: baselines_auto/bert-base-multilingual-cased/depth/L2

defaults:
  - _self_
  - /dataset: UD_Finnish-TDT/UD_Finnish-TDT
  - /embeddings: UD_Finnish-TDT/bert-base-multilingual-cased/L2
  - /probe: depth
  - /probe_rank@probe: "128"
  - /training: adam_modern_scheduler
  - /evaluation: default
  - /runtime: mps
  - /logging: UD_Finnish-TDT_bert-base-multilingual-cased/depth_L2

# Per-experiment training cadence (tests and real runs)
training:
  eval_every_n_epochs: 1
  eval_on_train_every_n_epochs: 40

evaluation:
  metrics: ["spearmanr_hm", "root_acc"]

logging:
  experiment_name: "UD_FINNISH-TDT_BERT-BASE-MULTILINGUAL-CASED_L2_DEPTH_R128"