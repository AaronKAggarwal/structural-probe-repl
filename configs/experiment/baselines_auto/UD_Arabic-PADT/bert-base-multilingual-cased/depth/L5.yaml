# Auto-generated by generate_configs.py
name: baselines_auto/bert-base-multilingual-cased/depth/L5

defaults:
  - _self_
  - /dataset: UD_Arabic-PADT/UD_Arabic-PADT
  - /embeddings: UD_Arabic-PADT/bert-base-multilingual-cased/L5
  - /probe: depth
  - /probe_rank@probe: "128"
  - /training: adam_modern_scheduler
  - /evaluation: default
  - /runtime: mps
  - /logging: UD_Arabic-PADT_bert-base-multilingual-cased/depth_L5

# Per-experiment training cadence (tests and real runs)
training:
  eval_every_n_epochs: 1
  eval_on_train_every_n_epochs: 40

evaluation:
  metrics: ["spearmanr_hm", "root_acc"]

logging:
  experiment_name: "UD_ARABIC-PADT_BERT-BASE-MULTILINGUAL-CASED_L5_DEPTH_R128"