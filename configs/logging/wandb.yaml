# configs/logging/wandb.yaml
wandb:
  enable: true
  project: "structural-probes-modern"
  entity: "aggarwal-k-aaron"
  group: "stage3-full-probe-test"  # Groups all runs from this training session
  
  # These will be overridden by the specific experiment file
  name: "default_run_${now:%H-%M-%S}" 
  tags: ["stage3", "probe-test", "L7", "5epochs"]