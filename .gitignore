# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.egg-info/
pip-wheel-metadata/
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Virtual Environments ---
.env
.venv
env/*                 # ignore everything under env …
!env/                 # …but allow the directory itself
!env/Dockerfile.legacy_pt_cpu   # …and keep this single file
venv/
\ENV/
env.bak/
venv.bak/

# --- IDE / Editor / OS Files ---
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.AppleDouble
*.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Planning ---
docs/MILESTONE_*

# --- Testing / Coverage ---
.pytest_cache/
.coverage
*.cov
nosetests.xml
coverage.xml
htmlcov/
.tox/
.nox/
scripts/train_probe_filtered.py


# --- Data Staging / Large Files ---
/data_staging/          # Ignore downloaded full datasets and generated sample data/embeddings
data_staging/
/data/
data/
data/
*.hdf5                  # Generally ignore large HDF5 files unless explicitly tracked
*.h5
*.params                # Ignore downloaded model parameters (like the .params from H&M)

# --- Results / Outputs ---
/results_staging/       # Ignore locally mounted results from container runs
results_staging/
/results/               # General results directory - adjust if needed
results/
*.log                   # Ignore log files by default
*.tsv
*.csv
*.png                   # Ignore generated images/plots by default
*.pdf                   # Ignore generated PDFs (like TikZ output) by default
*.tikz                  # Ignore generated TikZ files
*.observations
*.predictions
*.spearmanr
*.spearmanr-5-50-mean   # Or use *.spearmanr*
*.uuas
*.root_acc
*.pt
/checkpoints/
/outputs/
/outputs_extract_embeddings/
/outputs_extract_embeddings_filtered/
/multirun/
*.log
metrics_summary.json
commits_graph.txt
directory_tree.txt
# --- Specific to H&M Legacy Code Outputs ---
# Ignore the results dir within the legacy code structure too
src/legacy/structural_probe/example/results/ 

# --- Poetry ---
# poetry.lock should typically be committed, so DO NOT ignore it.
# Ignore poetry cache / config if necessary (usually not needed)
# .poetry/

# --- Other ---
*.ipynb_checkpoints
*.DS_Store
.cursorignore

# --- Secrets ---
# (Add any files containing secrets, keys, passwords here - none expected for this project)
# credentials.yaml
# *.env


# ---  Old Backups ---
*.bak
*.old
*.backup
*.save
*.tmp
*.temp
/temp*/
